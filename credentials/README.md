# Credentials

This directory is ignored by git.

Prefer Cloudflare bindings, Wrangler environment variables, and GitHub repository secrets for real credentials. For this Worker app, D1 is accessed through the `DB` binding and does not need a runtime endpoint credential file.
